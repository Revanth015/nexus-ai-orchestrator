# Resource Intelligence

Tracks worker availability, provider-confirmed quota where available, NEXUS-observed usage, rate-limit events, cost safety state, and bounded estimates when provider quota is not directly exposed.

Free-only mode is a hard safety gate.
