# Learning

NEXUS records worker and workflow performance after executions.

Initial V1 learning is deliberately transparent: task success, quality, accuracy, instruction following, reliability, efficiency, workflow quality, and resource safety. The system will use bounded exploration to discover better worker combinations without blindly consuming free resources.
