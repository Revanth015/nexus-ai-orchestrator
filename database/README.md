# Database

V1 uses SQLite locally so the NEXUS core can run without a hosted database or paid service.

Planned records include workers, resource observations, missions, tasks, task dependencies, artifacts, handoffs, quality reviews, checkpoints, and workflow experiments.
